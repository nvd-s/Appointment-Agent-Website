import { a as KalravWidget, K as KalravConfig } from './types-CUzovLlE.mjs';

/**
 * Kalrav AI Widget Core Implementation
 *
 * This is the core vanilla JS/TS implementation that works across all frameworks.
 * Enhanced version of the original kalravPureJs.js with TypeScript support.
 */
declare class Kalrav implements KalravWidget {
    private loaded;
    private script;
    private runtimeConfigSent;
    private lifecycleVersion;
    private readonly instanceId;
    private config;
    constructor(config: KalravConfig);
    /**
     * Remove broken or duplicate scripts
     */
    private removeStaleScripts;
    /**
     * Load the Kalrav widget
     */
    load(): Promise<void>;
    /**
     * Unload the widget
     */
    unload(): void;
    /**
     * Open the chat widget
     */
    open(): void;
    /**
     * Close the chat widget
     */
    close(): void;
    /**
     * Check if widget is loaded
     */
    isLoaded(): boolean;
    /**
     * Call any widget method
     */
    call(method: string, ...args: any[]): void;
}
/**
 * Factory function to create a new Kalrav instance
 */
declare function createKalrav(config: KalravConfig): KalravWidget;

export { Kalrav, KalravConfig, KalravWidget, createKalrav };
